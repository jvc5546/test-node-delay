deploy:
  compose: docker-compose.yml

dev:
  frontend:
    command: npm start
    resources:
      requests:
        cpu: "3.5Gi"
        memory: "12Gi"
      limits:
        cpu: "3.92Gi"
        memory: "13.5Gi"