deploy:
  compose: docker-compose.yml

dev:
  frontend:
    command: npm start
    resources:
      requests:
        cpu: "3.5GiB"
        memory: "12GiB"
      limits:
        cpu: "3.92GiB"
        memory: "13.5GiB"